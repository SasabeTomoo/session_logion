<h3>以下の内容で、送信する</h3>
<%= form_with(model: @blog, local: true) do |form| %>
  <%= form.hidden_field :name %>
  <%= form.hidden_field :content %>
  <p>名前：<%= @blog.name %></p>
  <p>内容：<%= @blog.content %></p>
  <%= form.submit  "登録する" %>
<% end %>
